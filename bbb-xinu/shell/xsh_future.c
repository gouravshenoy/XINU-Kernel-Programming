#include <future.h>

/*------------------------------------------------------------------------
 * xsh_future - Futures implementation
 *------------------------------------------------------------------------
 */
shellcmd xsh_future(int nargs, char *args[])
{
	/*
	queue q;
	sid32 sem;
	sem = semcreate(1);
	
	wait(sem);
	myenqueue(&q, 10);
	signal(sem);
	
	wait(sem);
	myenqueue(&q, 20);
	signal(sem);

	wait(sem);
	myenqueue(&q, 30);
	signal(sem);
	
	wait(sem);
	printf("dequeue value = %d \n", mydequeue(&q));
	signal(sem);

	
	wait(sem);
	printf("dequeue value = %d \n", mydequeue(&q));
	signal(sem);
	
	wait(sem);
	myenqueue(&q, 40);
	signal(sem);

	wait(sem);
	printf("dequeue value = %d \n", mydequeue(&q));
	signal(sem);

	wait(sem);
	printf("dequeue value = %d \n", mydequeue(&q));
	signal(sem);
	

	while(q.qnext != NULL) {
		wait(sem);
		printf("dequeue value = %d \n", mydequeue(&q));
		signal(sem);
	}
	*/
}

	
